package thaumcraft.common.lib.network;

/**
 * Legacy 1.12 placeholder retained only to avoid compile grief when porting.
 * All networking is handled by 1.16.5 {@link thaumcraft.common.network.NetworkHandler}.
 * This class is intentionally empty and will be deleted once all callers are ported.
 */
public final class PacketHandler { private PacketHandler() {} }

