buildscript {
    repositories {
        maven { url = 'https://maven.minecraftforge.net' }
        mavenCentral()
    }
    dependencies {
        classpath group: 'net.minecraftforge.gradle', name: 'ForgeGradle', version: '5.1.+', changing: true
    }
}

apply plugin: 'net.minecraftforge.gradle'
apply plugin: 'eclipse'
apply plugin: 'maven-publish'

version = '1.16.5-7.0.0.0'
group = 'thaumcraft' 
archivesBaseName = 'Thaumcraft'

java.toolchain.languageVersion = JavaLanguageVersion.of(8)

minecraft {
    mappings channel: 'official', version: '1.16.5'

    runs {
        client {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            property 'forge.logging.console.level', 'debug'
            mods {
                thaumcraft {
                    source sourceSets.main
                }
            }
        }

        server {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            property 'forge.logging.console.level', 'debug'
            mods {
                thaumcraft {
                    source sourceSets.main
                }
            }
        }

        data {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            property 'forge.logging.console.level', 'debug'
            args '--mod', 'thaumcraft', '--all', '--output', file('src/generated/resources/'), '--existing', file('src/main/resources/')
            mods {
                thaumcraft {
                    source sourceSets.main
                }
            }
        }
    }
}

// Temporarily exclude legacy (1.12-style) theorycraft implementations while porting
sourceSets {
    main {
        java {
            exclude 'thaumcraft/common/lib/research/theorycraft/**'
            // Exclude legacy 1.12 utils until ported
            exclude 'thaumcraft/common/lib/utils/**'
            // Explicit legacy files (kept for clarity during port)
            exclude 'thaumcraft/common/lib/utils/BlockStateUtils.java'
            exclude 'thaumcraft/common/lib/utils/BlockUtils.java'
            exclude 'thaumcraft/common/lib/utils/CropUtils.java'
            exclude 'thaumcraft/common/lib/utils/EntityUtils.java'
            exclude 'thaumcraft/common/lib/utils/InventoryUtils.java'
            exclude 'thaumcraft/common/lib/utils/Utils.java'
            // Exclude legacy arcane workbench (new tiles/menus live under common/)
            exclude 'thaumcraft/world/crafting/arcane/**'
            exclude 'thaumcraft/registry/Registration.java'
            exclude 'thaumcraft/api/casters/**'
            exclude 'thaumcraft/api/aspects/AspectSourceHelper.java'
            // Messages referencing casters/focus engine (deferred)
            exclude 'thaumcraft/common/network/msg/RequestFocusChangeMessage.java'
            exclude 'thaumcraft/common/network/msg/RequestItemKeyMessage.java'
        }
    }
}

repositories {
    maven {
        name = "Progwml6 maven"
        url = "https://dvs1.progwml6.com/files/maven/"
    }
    maven {
        name = "Jared's maven"
        url = "https://maven.blamejared.com/"
    }
    maven {
        name = "ModMaven"
        url = "https://modmaven.k-4u.nl"
    }
}

dependencies {
    minecraft 'net.minecraftforge:forge:1.16.5-36.2.39'
}

jar {
    manifest {
        attributes([
            "Specification-Title": "thaumcraft",
            "Specification-Vendor": "azanor",
            "Specification-Version": "1", // We are version 1 of ourselves
            "Implementation-Title": project.name,
            "Implementation-Version": "${version}",
            "Implementation-Vendor" :"azanor",
            "Implementation-Timestamp": new Date().format("yyyy-MM-dd'T'HH:mm:ssZ")
        ])
    }
}

jar.finalizedBy('reobfJar')

publishing {
    publications {
        mavenJava(MavenPublication) {
            artifact jar
        }
    }
    repositories {
        maven {
            url "file:///${project.getProjectDir()}/mcmodsrepo"
        }
    }
}


